// The Graph message contains a state transition graph represented as states
// and possible state transitions
namespace app.stateGraph {
    message Graph {
        string startingStateCode
        map<string State> states
    }
}
