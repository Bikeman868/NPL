namespace app.interface {
    // The response to a request for the entity state
    message StateResponse {
        string entityKey
        date timestamp
        string stateCode
        string stateName
    }
}
